.env
.env.local
/venv/
__pycache__/
*.pyc
*.egg-info/
logs/
.claude/settings.local.json

# Knowledge-base chunks (kept in a separate private dataset repo)
# Local copy is required for python_llm strategy with RAG; agent gracefully degrades if absent.
data/chunks/

# Historical OHLCV downloads (large CSVs, generated by scripts/backtest.py --source ccxt)
data/historical/

# LLM response cache (private)
data/llm_cache/

# ERC-8004 on-chain state
logs/erc8004/

# Local wallet generation script (must be deleted after use)
gen_wallet.py